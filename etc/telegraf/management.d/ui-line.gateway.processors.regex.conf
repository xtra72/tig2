# Telegraf Configuration
# 처리 방법이 같은 것들을 솎아서 여기에 모아둔다. 
[[processors.regex]]
  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st01/[^/.]+/gateway/.+$'
    replacement = "신설역"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st02/[^/.]+/gateway/.+$'
    replacement = "보문"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st03/[^/.]+/gateway/.+$'
    replacement = "성신여대입구"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st04/[^/.]+/gateway/.+$'
    replacement = "정릉"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st05/[^/.]+/gateway/.+$'
    replacement = "북한산보국문"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st06/[^/.]+/gateway/.+$'
    replacement = "솔샘"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st07/[^/.]+/gateway/.+$'
    replacement = "삼양사거리"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st08/[^/.]+/gateway/.+$'
    replacement = "삼양"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st09/[^/.]+/gateway/.+$'
    replacement = "화계"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st10/[^/.]+/gateway/.+$'
    replacement = "가오리"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st11/[^/.]+/gateway/.+$'
    replacement = "4.19민주묘지"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st12/[^/.]+/gateway/.+$'
    replacement = "솔밭공원"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/st13/[^/.]+/gateway/.+$'
    replacement = "북한산우이"
    result_key = "station"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/01/gateway/.+$'
    replacement = "상행계단#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/02/gateway/.+$'
    replacement = "상행계단#2"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/03/gateway/.+$'
    replacement = "상행계단#3"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/04/gateway/.+$'
    replacement = "하행계단#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/05/gateway/.+$'
    replacement = "하행계단#2"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/06/gateway/.+$'
    replacement = "하행계단#3"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/07/gateway/.+$'
    replacement = "상행#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/08/gateway/.+$'
    replacement = "상행#2"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/09/gateway/.+$'
    replacement = "상행#3"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/10/gateway/.+$'
    replacement = "하행#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/11/gateway/.+$'
    replacement = "하행#2"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/12/gateway/.+$'
    replacement = "하행#3"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/13/gateway/.+$'
    replacement = "대합실#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/14/gateway/.+$'
    replacement = "대합실#2"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/15/gateway/.+$'
    replacement = "대합실#3"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/16/gateway/.+$'
    replacement = "환승통로#1"
    result_key = "spot"

  [[processors.regex.tags]]
    key = "topic"
    pattern = '^(data|state)/ui-line/(?P<station>[^/.]+)/17/gateway/.+$'
    replacement = "연결통로#1"
    result_key = "spot"

